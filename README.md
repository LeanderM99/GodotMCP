# GodotMCP

MCP server + C# EditorPlugin for controlling Godot 4.6+ from Claude Code.

## Components

- `mcp-server/` — TypeScript MCP server (stdio transport)
- `godot-plugin/` — C# EditorPlugin (WebSocket server)
